services:
  postgres:
    image: postgres:15
    container_name: order_api_final
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: password
      POSTGRES_DB: orderdb
    ports:
      - "5434:5432"
    volumes:
      - pgdata_final:/var/lib/postgresql/data
volumes:
  pgdata_final: